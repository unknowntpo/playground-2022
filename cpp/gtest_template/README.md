Set up CMake 

```
$ cmake -S . -B build
```

Build the project:

```
$ cmake --build build
```


Run Tests

```
$ cd build && ctest

```

Run Tests with verbose mode

```
$ cd build && ctest --verbose

```
