# Enable container reuse across test runs  
testcontainers.reuse.enable=true

# Configure cleanup behavior
testcontainers.cleanup.strategy=ON_SUCCESS

# Reduce startup timeout for faster tests
testcontainers.startup.timeout=120