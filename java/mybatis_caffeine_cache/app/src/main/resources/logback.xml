<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- MyBatis logging -->
    <logger name="org.apache.ibatis" level="DEBUG"/>
    <logger name="org.apache.ibatis.cache" level="DEBUG"/>
    <logger name="org.example.mapper" level="DEBUG"/>
    
    <!-- Caffeine cache logging -->
    <logger name="org.mybatis.caches.caffeine" level="DEBUG"/>
    <logger name="com.github.benmanes.caffeine" level="DEBUG"/>
    
    <!-- HikariCP connection pool logging -->
    <logger name="com.zaxxer.hikari" level="DEBUG"/>
    
    <!-- Jersey logging (optional, can be noisy) -->
    <logger name="org.glassfish.jersey" level="INFO"/>
    
    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>